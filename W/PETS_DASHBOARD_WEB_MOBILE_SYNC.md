# My Pets + Pet Owner Dashboard Sync

Mobile My Pets now reads/writes the same `public.pets` rows used by the web.
The Pet Owner Dashboard now reads live pets, appointments, finalized medical records,
and personal queue data from the shared Supabase project.

Run `SUPABASE_PETS_DASHBOARD_REALTIME_SYNC.sql` once in Supabase SQL Editor.
